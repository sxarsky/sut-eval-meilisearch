# Generated by Skyramp v1.3.30 on 2026-06-30 00:15:04.768988304 +0000 UTC m=+338.862199022
# Command: skyramp generate contract rest http://localhost:7700/health \
# 		--framework pytest \
# 		--language python \
# 		--method GET \
# 		--output health_contract_test.py \
# 		--output-dir /home/runner/work/sut-eval-meilisearch/sut-eval-meilisearch/tests/skyramp \
# 		--overwrite \
# 		--provider \
# 		--response-data {"status":"available"} \
# 		--response-status-code 200 \
# 		--runtime local \
# 		--skipProvisionParents

# Import of required libraries
import skyramp
import os
import time


# URL for test requests
URL = skyramp.get_base_url("SKYRAMP_TEST_BASE_URL", "http://localhost:7700")

# contract test for /health GET
def test_health_get():
    # Invocation of Skyramp Client
    client = skyramp.Client()
    # Definition of authentication header
    headers = {}
    if os.getenv("SKYRAMP_TEST_TOKEN") is not None:
        headers["Authorization"] = "Bearer " + os.getenv("SKYRAMP_TEST_TOKEN")

    # Expected Response Body
    expected_health_GET_response_body = r'''{
        "status": "available"
    }'''

    # Execute Request
    health_GET_response = client.send_request(
        url=URL,
        path="/health",
        method="GET",
        headers=headers
    )

    # Generated Assertions
    assert health_GET_response.status_code == 200
    assert skyramp.check_schema(health_GET_response, expected_health_GET_response_body)
    assert skyramp.get_response_value(health_GET_response, "status") is not None
    assert skyramp.get_response_value(health_GET_response, "status") == "available"


if __name__ == "__main__":
    test_health_get()
