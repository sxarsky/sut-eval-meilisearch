# Generated by Skyramp v1.3.37 on 2026-08-09 04:15:24.807421451 +0000 UTC m=+368.155071517
# Command: skyramp generate contract rest http://host.docker.internal:7700/version \
# 		--auth-type Bearer \
# 		--framework pytest \
# 		--language python \
# 		--method GET \
# 		--output-dir /home/runner/work/sut-eval-meilisearch/sut-eval-meilisearch/tests/skyramp \
# 		--overwrite \
# 		--provider \
# 		--response-data {"commitSha":"abc123","commitDate":"2024-01-01","pkgVersion":"1.0.0","edition":"community"} \
# 		--response-status-code 200 \
# 		--runtime local

# Import of required libraries
import skyramp
import os


# URL for test requests
URL = skyramp.get_base_url("SKYRAMP_TEST_BASE_URL", "http://host.docker.internal:7700")

# contract test for /version GET
def test_version_get():
    # Invocation of Skyramp Client
    client = skyramp.Client()
    # Definition of authentication header
    headers = {}
    if os.getenv("SKYRAMP_TEST_TOKEN") is not None:
        headers["Authorization"] = "Bearer " + os.getenv("SKYRAMP_TEST_TOKEN")

    # Expected Response Body
    expected_version_GET_response_body = r'''{
        "commitDate": "2024-01-01",
        "commitSha": "abc123",
        "edition": "community",
        "pkgVersion": "1.0.0"
    }'''

    # Execute Request
    version_GET_response = client.send_request(
        url=URL,
        path="/version",
        method="GET",
        headers=headers
    )

    # Generated Assertions
    assert version_GET_response.status_code == 200
    assert skyramp.check_schema(version_GET_response, expected_version_GET_response_body)
    assert skyramp.get_response_value(version_GET_response, "commitDate") is not None
    assert skyramp.get_response_value(version_GET_response, "commitSha") is not None
    assert skyramp.get_response_value(version_GET_response, "edition") == "community"


if __name__ == "__main__":
    test_version_get()
