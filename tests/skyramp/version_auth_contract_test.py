# Generated by Skyramp v1.3.30 on 2026-06-30 00:15:01.790541032 +0000 UTC m=+335.883751740
# Command: skyramp generate contract rest http://localhost:7700/version \
# 		--auth-type Bearer \
# 		--framework pytest \
# 		--language python \
# 		--method GET \
# 		--output version_auth_contract_test.py \
# 		--output-dir /home/runner/work/sut-eval-meilisearch/sut-eval-meilisearch/tests/skyramp \
# 		--overwrite \
# 		--provider \
# 		--response-status-code 401 \
# 		--runtime local \
# 		--skipProvisionParents

# Import of required libraries
import skyramp
import os
import time


# URL for test requests
URL = skyramp.get_base_url("SKYRAMP_TEST_BASE_URL", "http://localhost:7700")

# contract test for /version GET
def test_version_get():
    # Invocation of Skyramp Client
    client = skyramp.Client()
    # No auth header — testing that endpoint returns 401 when unauthenticated
    headers = {}

    # Execute Request
    version_GET_response = client.send_request(
        url=URL,
        path="/version",
        method="GET",
        headers=headers
    )

    # Generated Assertions
    assert version_GET_response.status_code == 401
    assert skyramp.get_response_value(version_GET_response, "code") == "missing_authorization_header"
    assert skyramp.get_response_value(version_GET_response, "type") == "auth"
    assert skyramp.get_response_value(version_GET_response, "message") is not None
    assert skyramp.get_response_value(version_GET_response, "link") is not None


if __name__ == "__main__":
    test_version_get()
